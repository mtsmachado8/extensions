{
  "packages": [
    "packages/**"
  ],
  "command": {
    "publish": {
      "ignoreChanges": ["ignored-file", "*.md"],
      "message": "chore(release): publish",
      "allowBranch": "master"
    }
  },
  "version": "independent",
  "npmClient": "yarn",
  "useWorkspaces": true
}
