{
  "packages": [
    "packages/**"
  ],
  "command": {
    "publish": {
      "ignoreChanges": ["ignored-file", "*.md"],
      "message": "chore(release): publish",
      "allowBranch": "develop"
    }
  },
  "version": "independent",
  "npmClient": "yarn",
  "useWorkspaces": true
}
